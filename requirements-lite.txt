# Lightweight requirements for serverless deployments (Vercel)
# Excludes heavy packages like tensorflow and xgboost. Keep this file for deployments
# where full ML libraries are not required or are hosted separately.
# Pinned versions for Python 3.11 compatibility on Vercel
Flask==2.2.5
Flask-Cors==3.0.10
joblib==1.2.0
numpy==1.24.3
pandas==1.5.3
scikit-learn==1.2.2
matplotlib==3.7.1
seaborn==0.12.2

# XAI libraries for explainability features (SHAP and LIME)
shap==0.41.0
lime==0.2.0.1

# Dependencies for SHAP and LIME
scipy==1.10.1
numba==0.56.4
tqdm==4.65.0
